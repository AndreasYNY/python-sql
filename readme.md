# Using this
- install the requirements using `pip install -r requirement.txt` and run `uvicorn main:app`.
- in the `ext` folder there are the database scheme that needed for mysql
- i forgot to use .env for the connection, so please do update `util/connection.py` (it's 11:00)

# routes
Checkout `/docs` on port `8080`

# Whats this?
Demo app to demonstrate my SQL syntax and python knowlege (albeit not much).

There are a lot of boiler plate since I cant figure out how to write wrapper function...